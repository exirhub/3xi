# Pinned terminal menu

`x-ui.sh` is from `MHSanaei/3x-ui`, tag `v3.8.5`, path `x-ui.sh`.

Source: https://raw.githubusercontent.com/MHSanaei/3x-ui/v3.8.5/x-ui.sh

SHA-256: see `../../upstream.lock.json`. Installation checks its hash and shell syntax, then installs it at `/usr/bin/x-ui`. The menu and panel archive use the same upstream version. See `LICENSE`.

Use `3xi renew` for the managed frontend certificate; leave internal panel/subscription certificate settings empty. Upstream menu actions that reinstall/uninstall the panel or change management paths are not equivalent to a 3xi deployment update.
